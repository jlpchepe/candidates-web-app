<Window x:Class="ReclutaCV.Ventanas.MenuPrincipal.MenuPrincipalView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReclutaCV.Ventanas.MenuPrincipal"
        mc:Ignorable="d"
        Title="Menú principal" SizeToContent="Height"
        Width="450"
    >
    <StackPanel Background="{StaticResource ThirdColor}">
        <Border>
            <TextBlock Style="{StaticResource TitleTextBlock}">Gestión de candidatos</TextBlock>
        </Border>
        <StackPanel Style="{StaticResource DefaultStackpanelStyle}" Background="Aquamarine">
            <Button Command="{Binding VerListadoCandidatos}">Candidatos</Button>
            <Button Command="{Binding VerListadoDeSolicitudDeVacantes}">Solicitudes de vacantes</Button>
        </StackPanel>
    </StackPanel>
</Window>
